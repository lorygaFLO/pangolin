# Registry for the raw validation step.
# Keys are glob patterns matched against input file names; each entry lists
# the validators to apply (with their parameters).
#
# Example:
# "*sales*":
#   validators:
#     is_empty_dataframe:
#     required_columns:
#       - product_id
#       - price
#       - date
