 - dispatcher should be able to save a a file with match "*FR*sales*.csv" 
 in netsted folder structure like FR\SALES\sales_filename.csv

 - when the output file is produced the system should save take a varibel 
 that indicate if saving with the original one, always in csv or as parquet.
 we could insert a parameter in the pipeline to define to define that or in settings or both

 - when in pipeline structure the input folder is deafult the systemuser the input folder indicated in the settings.

 - evaluate _settings_key in data structure. theorically is a good idea.

 - evaluate -path parameter in data structure. what is used for?

 - make pangolin able to deal with cloud environments

 - Implement a testing strategy

 - Create a pipeline Class hat make sure the project is correctly setted