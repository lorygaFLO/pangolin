# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# Environments
.env
.env.*
.venv/
venv/

# Pangolin runtime data (never commit pipeline data)
data/

# Local Prefect state (server DB/profile) — see PREFECT_HOME in .env
.prefect/
